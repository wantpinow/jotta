# Jotta Modal Deployments

This folder contains code to deploy various ML models on [Modal](https://modal.com). The `/development` folder contains Modal apps for testing, development, and single-use jobs. The `/production` folder contains Modal apps for production use. Apps from the `/production` folder are automatically deployed to Modal via GitHub Actions.
